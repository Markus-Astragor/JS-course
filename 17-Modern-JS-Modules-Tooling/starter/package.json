{
  "name": "starter",
  "version": "1.0.0",
  "description": "- module it is a reusable piece of code that hides implementation details (so we don't see how it is created)\r - usually standalone file but not necessary",
  "main": "clean.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "core-js": "^3.32.1",
    "leaflet": "^1.9.4",
    "lodash-es": "^4.17.21",
    "parcel-bundler": "^1.12.5",
    "regenerator-runtime": "^0.14.0"
  }
}
